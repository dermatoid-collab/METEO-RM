package it.meteoapp.clone.ui.theme

import androidx.compose.ui.graphics.Color

// ── Background blues (dal gradiente MeteoBlue) ──────────────────────────────
val BackgroundDeep     = Color(0xFF0D1B2A)   // blu notte profondo
val BackgroundMid      = Color(0xFF1A2E45)   // blu navy
val BackgroundCard     = Color(0xFF1E3554)   // blu card
val BackgroundCardAlt  = Color(0xFF162840)   // card alternativa

// ── Tab / Surface ────────────────────────────────────────────────────────────
val TabSelected        = Color(0xFF2A5298)   // blu tab selezionato
val TabUnselected      = Color(0xFF1A2E45)

// ── Testo ────────────────────────────────────────────────────────────────────
val TextPrimary        = Color(0xFFFFFFFF)
val TextSecondary      = Color(0xFFB0C4DE)   // azzurro chiaro
val TextMuted          = Color(0xFF7A9BBF)

// ── Temperatura — scala termica (verde → giallo → arancio) ──────────────────
val TempHot            = Color(0xFFFFD700)   // giallo caldo (max)
val TempMild           = Color(0xFF9ACD32)   // verde lime (min)
val TempCold           = Color(0xFF87CEEB)   // azzurro (freddo)

// ── Precipitazioni ───────────────────────────────────────────────────────────
val PrecipBlue         = Color(0xFF4FC3F7)
val PrecipHeavy        = Color(0xFF0288D1)

// ── UV index ─────────────────────────────────────────────────────────────────
val UvLow              = Color(0xFF4CAF50)
val UvMid              = Color(0xFFFFEB3B)
val UvHigh             = Color(0xFFFF9800)
val UvVeryHigh         = Color(0xFFF44336)

// ── Accent ───────────────────────────────────────────────────────────────────
val AccentBlue         = Color(0xFF4A90D9)
val AccentGlow         = Color(0xFF2196F3)

// ── Radar thumbnail ──────────────────────────────────────────────────────────
val RadarGreen         = Color(0xFF26A69A)
val RadarDark          = Color(0xFF1B3A35)
